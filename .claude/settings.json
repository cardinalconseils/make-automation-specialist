{
  "enabledPlugins": {
    "make-automation-specialist@make-automation-specialist": true
  },
  "//": "Hooks are registered by the plugin itself in hooks/hooks.json using ${CLAUDE_PLUGIN_ROOT}, so they fire in every project the plugin is enabled in — not just this repo. Do not re-add absolute-path hook entries here; they would double-fire when working inside this repo."
}
